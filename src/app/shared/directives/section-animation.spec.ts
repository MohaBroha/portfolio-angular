import { SectionAnimation } from './section-animation';

describe('SectionAnimation', () => {
  it('should create an instance', () => {
    const directive = new SectionAnimation();
    expect(directive).toBeTruthy();
  });
});
